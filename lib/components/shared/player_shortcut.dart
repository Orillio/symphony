import 'package:flutter/material.dart';

class PlayerShortcut extends StatefulWidget {
  const PlayerShortcut({Key? key}) : super(key: key);

  @override
  State<PlayerShortcut> createState() => _PlayerShortcutState();
}

class _PlayerShortcutState extends State<PlayerShortcut> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
